<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD  Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.dealight.mapper.WaitingMapper">


	<select id="findAll" resultType="com.dealight.domain.WaitingVO">
		SELECT
		*
		FROM tbl_wait
	</select>

	<select id="findById" resultType="com.dealight.domain.WaitingVO">
		SELECT
		*
		FROM tbl_wait
		where id =
		#{id}
	</select>

	<insert id="insert">
		INSERT
		INTO
		tbl_wait(id, store_id, user_id,
		wait_reg_tm,wait_pnum,cust_telno,cust_nm,wait_stus_cd)
		VALUES(seq_wait.nextval,#{storeId},#{userId,jdbcType=NUMERIC},#{waitRegTm},#{waitPnum},#{custTelno},#{custNm},#{waitStusCd})

	</insert>

	<insert id="insertSelectKey">
		<selectKey keyProperty="id" order="BEFORE" resultType="long">
			select seq_wait.nextval from dual
		</selectKey>
		INSERT
		INTO
		tbl_wait(id, store_id, user_id,
		wait_reg_tm,wait_pnum,cust_telno,cust_nm,wait_stus_cd)
		VALUES(#{id},#{storeId},#{userId,jdbcType=NUMERIC},#{waitRegTm},#{waitPnum},#{custTelno},#{custNm},#{waitStusCd})

	</insert>



	<update id="update">
		UPDATE tbl_wait
		SET
		store_id = #{storeId},
		user_id =
		#{userId,jdbcType=NUMERIC},
		wait_reg_tm = #{waitRegTm},
		cust_telno =
		#{custTelno},
		cust_nm = #{custNm},
		wait_stus_cd = #{waitStusCd}
		where id
		= #{id}
	</update>

	<delete id="delete">
		DELETE
		FROM tbl_wait
		WHERE id = #{id}
	</delete>


</mapper>